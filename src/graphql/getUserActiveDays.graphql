query ($username: String!, $year: Int!) {
    matchedUser(username: $username) {
        userCalendar(year: $year) {
            activeYears
            streak
            submissionCalendar
            totalActiveDays
        }
    }
}

# Variable
# {
#     "username": string,
#     "year": int
# }