query ($username: String!) {
    matchedUser(username: $username) {
        userCalendar {
            activeYears
            streak
            submissionCalendar
            totalActiveDays
        }
    }
}

# Variable
# {
#     "username": string
# }